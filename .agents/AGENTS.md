# Project Rules & Guidelines

## Git Branching Policy
- **NEVER push directly to `main` branch.**
- Always work in dedicated feature branches (e.g. `feature/...` or `dev/...`).
- All commits and pushes must be made to the active `feature/...` branch.
- Merging to `main` requires explicit user confirmation.
