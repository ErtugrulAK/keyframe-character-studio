fix: calculate gizmo rotation ring radius dynamically per element type bounds

- Replaced static gizmo radius with getPartBoundingRadius(selectedPart)
- Dynamically measures element bounding box (UI Card: 180x110px, Banner: 220x60px, Media: 200x140px, Shapes: 60x60px)
- Ensures the selection gizmo circle floats generously 16px OUTSIDE element borders for UI cards, images, and videos without cutting through text or internal elements
