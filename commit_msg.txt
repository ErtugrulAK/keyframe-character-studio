feat: add real-time zoom counter-scaling for viewport typography, origin markers, and gizmos

- Added zScale = 1 / Math.max(0.15, zoomLevel) counter-scaling formula to StageCanvas SVG viewport
- Origin label '(0,0) ORIGIN', axes lines, grid marks, and bone links dynamically expand as the viewport zooms out so text remains 100% crisp, sharp, and readable at all zoom levels
- Gizmo transform handles, rotation degree badge, and move arrows scale counter-proportionally to zoom out level so controls never turn into microscopic dots
