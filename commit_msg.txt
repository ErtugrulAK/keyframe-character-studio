fix: mount Curve Studio Modal to document.body via React Portal with top z-index

- Fixed z-index layering bug where the timeline bottom bar rendered on top of the modal window
- Used ReactDOM.createPortal(..., document.body) with z-index: 999999 so the modal floats cleanly on top of all application panels
- Added responsive max-height: calc(100vh - 40px) and vertical scrolling to prevent bottom clipping on smaller screens
