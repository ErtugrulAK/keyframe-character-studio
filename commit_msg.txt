fix: smooth distance-ratio scaling math, add Rectangle shape, and non-scaling-stroke outline

- Removed hardcoded 3.0 scale limit in StageCanvas and implemented smooth distance-ratio scaling math up to 50x
- Added custom_rect (Rectangle) shape item to LeftToolbar, animator types, and SVG renderer
- Added vectorEffect="non-scaling-stroke" to all shape elements so outline borders remain crisp 1.5px/2px regardless of object scale
