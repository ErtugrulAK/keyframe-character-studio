feat: Unreal Engine Sequencer-style timeline with per-property channel keyframing

- Added PropertyKeyframe and TrackChannel types to animator.ts
- Added channels field (x/y/rotation/scaleX/scaleY/opacity) to Track type
- makeEmptyChannels and interpolateChannel helpers in defaults.ts
- getComputedTransform reads per-channel data first, falls back to composite keyframes
- toggleTrackExpanded, addPropertyKeyframe, deletePropertyKeyframe, updatePropertyKeyframeFrame actions
- migrateTrack helper auto-patches old saves with empty channels on load
- Full Unreal-style SequencerTimeline: collapsible hierarchy, channel lanes, color-coded diamonds
