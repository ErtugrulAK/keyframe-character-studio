fix: flip Y axis display in Inspector to match Cartesian coordinate convention

- Negated Position Y value in display (value = -transform.y) so that upward direction shows positive numbers
- Negated input on change (y: -val) so that typing a positive Y value correctly moves the object upward on screen
- Added label indicator "POSITION Y ↑+" to clarify convention for users
