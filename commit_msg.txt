feat: Unreal Engine Sequencer collapsible property sub-groups and trajectory keyframe lines

- Organized Transform properties into collapsible sub-groups matching Unreal Sequencer: Location (X, Y), Rotation, Scale (Scale X, Scale Y), Opacity
- Added interactive sub-group toggle headers with chevrons so properties are not all flatly exposed at once
- Added horizontal trajectory connecting lines across keyframes in each channel lane matching Unreal Sequencer keyframe track visualization
- Added clean table grid borders across outliner and timeline grid lanes
