feat: allow clicking and dragging anywhere on element body to move it directly

- Replaced simple onClick with onMouseDown on SVG element path rendering
- Immediately triggers translate move drag when clicking anywhere on an element's body (rectangle, box, card, banner, text, image, video, etc.)
- Users no longer need to hit the tiny gizmo center square knob to move objects on stage
