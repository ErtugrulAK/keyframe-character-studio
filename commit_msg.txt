fix: support clean primary left-click selection and instant drag translation

- Added startTranslateDragForPart helper retrieving target element transform dynamically on mousedown
- Preserved primary left-click (e.button === 0) selection on element body
- Ensures left-clicking selects the target element cleanly, while left-drag instantly translates it without state delays
