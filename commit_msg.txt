fix: adjust gizmo rotation circle radius (r0) to wrap snugly around selected object bounds

- Adjusted base gizmo rotation ring radius from 70px down to 42px * objScale
- Aligned translation arrow lengths and scale handle diagonal line proportionally (arrowLen = r0 * 0.78, scaleLine = r0 * 0.68)
- Ensures the selection gizmo circle wraps tight and snug around all shapes, cards, and text layers without forming an oversized ring
