fix: add SmartNumberInput component for smooth typing with Enter/Blur submit

- Implemented SmartNumberInput component with local string state buffer
- Fixes issue where deleting text forced zero ('090' bug) or blocked typing negative values like '-45°'
- Replaced all numeric inputs across Transform and Style tabs (Position, Rotation, Opacity, Scale X/Y, Z-Index, Font Size, Shadow Blur/Offset)
- Supports selecting all text on focus, typing negative and decimal values freely, and submitting on Enter key press or Blur
