# Global OMP Tooling Status

This is a secret-free project status summary. Global credential stores, provider tokens, API keys, and full configuration dumps are intentionally excluded.

## Home PC

- OMP: `18.1.19`
- OpenCode: `1.18.14`
- Context7 MCP: READY; live documentation lookup passed after headless OpenCode device authorization.
- Playwright MCP: READY; smoke verification passed.
- Supabase CLI: installed and gated; no production connection.
- Strix: installed and gated; no scans run.
- Skill UI: installed and gated; no website crawling run.
- Task concurrency: `8`.
- Memory backend: `mnemopi`.
- Model roles: unchanged and matching the approved setup.
- Global guide: `C:\Users\senmu\.omp\GLOBAL_TOOLING_GUIDE.md`.

## Safety boundaries

- No authentication store contents or secrets are committed to this repository.
- No provider tokens or API keys are recorded here.
- Supabase production access remains gated.
- Strix scans and Skill UI website crawling remain gated.
- Project source, package files, release tags, and runtime configuration are outside this tooling checkpoint.

## Work PC handoff

The Work PC requires a separate safe synchronization pass for the repository `main` state and the global OMP tooling setup. Compare approved configuration values without copying credential stores.
